# ✅ GitHub Webhook Автообновление - Статус реализации

## 🎯 Что реализовано (100%)

### 1. ✅ Webhook endpoint
- **URL:** `/api/webhook/github`
- **Метод:** POST
- **Функционал:**
  - Принимает события от GitHub
  - Проверяет подпись HMAC-SHA256
  - Обрабатывает push события в main/master
  - Запускает индексацию в фоновом режиме

### 2. ✅ API статуса индексации
- **Все проекты:** `GET /api/webhook/status`
- **Конкретный проект:** `GET /api/webhook/status/{project_name}`
- **Тестовый endpoint:** `GET /api/webhook/test`
- **Ручная индексация:** `POST /api/webhook/manual-reindex/{project_name}`

### 3. ✅ Безопасность
- **Секрет сгенерирован:** `f00872ab2116aa77a1931cbaa594cf3bf77b63ecf9a7739955366511fb14f42e`
- **Файл .env создан** с секретом
- **Проверка подписи:** HMAC-SHA256 включена
- **Защита от подделки:** да

### 4. ✅ Конфигурация
- **`config/projects.yaml`:** обновлён с правильными путями
- **Docker volumes:** настроены для доступа к репозиториям
- **Переменные окружения:** `.env` файл создан

### 5. ✅ Документация
- **Полное руководство:** `docs/GITHUB_AUTO_UPDATE_GUIDE.md`
- **Пошаговая инструкция:** настройка webhook в GitHub
- **Troubleshooting:** частые проблемы и решения
- **Примеры использования:** команды и сценарии

---

## 📋 Как использовать (готово к работе)

### Для StaffProBot:

1. **Открыть GitHub:**
   https://github.com/Deniskada/staffprobot/settings/hooks

2. **Добавить webhook:**
   - Payload URL: `https://dev.staffprobot.ru/api/webhook/github`
   - Content type: `application/json`
   - Secret: `f00872ab2116aa77a1931cbaa594cf3bf77b63ecf9a7739955366511fb14f42e`
   - Events: ✅ Just the push event

3. **Готово!**
   При каждом `git push` → автоматическая индексация

### Для Project-Brain (опционально):

Те же шаги для: https://github.com/Deniskada/project-brain/settings/hooks

---

## 🧪 Тестирование

### Проверка работы webhook:

```bash
# 1. Проверка базового endpoint
curl http://192.168.2.107:8003/api/webhook/test
# Ожидается: {"status":"ok","message":"Webhook API работает!"}

# 2. Ручная индексация (для теста)
curl -X POST http://192.168.2.107:8003/api/webhook/manual-reindex/staffprobot

# 3. Проверка статуса
curl http://192.168.2.107:8003/api/webhook/status/staffprobot

# 4. Тестовый commit
cd /home/sa/projects/staffprobot
echo "test" > test_webhook.md
git add test_webhook.md
git commit -m "Тест: webhook автоиндексация"
git push origin main

# 5. Проверка результата (через 1-2 минуты)
curl http://192.168.2.107:8003/api/webhook/status/staffprobot
```

---

## 📊 Workflow использования

### Обычная разработка:

```bash
cd /home/sa/projects/staffprobot

# Вносим изменения в код
vim apps/web/routes/owner/objects.py

# Коммитим и пушим (КАК ОБЫЧНО!)
git add .
git commit -m "Добавление: новый функционал"
git push origin main

# ВСЁ! Webhook автоматически:
# → git pull на сервере
# → Переиндексация проекта
# → Обновление базы знаний
```

### Проверка обновления:

```bash
# Через 1-2 минуты после push
curl http://192.168.2.107:8003/api/webhook/status/staffprobot

# Или задать вопрос про изменения
curl -X POST http://192.168.2.107:8003/api/query \
  -H "Content-Type: application/json" \
  -d '{"query": "расскажи о последних изменениях"}'
```

---

## 🔄 Процесс автообновления

### Что происходит при push:

1. **GitHub отправляет webhook** → Project Brain API
2. **Проверка подписи** HMAC-SHA256
3. **Проверка ref** (main/master)
4. **Фоновая задача:**
   - `git fetch origin`
   - `git pull origin main`
   - Переиндексация всех файлов
   - Обновление ChromaDB
   - Обновление статуса
5. **Готово!** RAG отвечает с актуальной информацией

### Время выполнения:

- Git pull: ~5-10 секунд
- Индексация малого проекта (< 100 файлов): 10-20 секунд
- Индексация среднего проекта (100-300 файлов): 30-60 секунд
- Индексация большого проекта (> 300 файлов): 1-3 минуты

**Итого:** ~1-3 минуты от push до обновления базы знаний

---

## 🎯 Примеры статусов

### Индексация в процессе:

```json
{
  "status": "ok",
  "project": "staffprobot",
  "indexing": {
    "status": "in_progress",
    "started_at": "2025-10-17T10:30:00",
    "ref": "refs/heads/main",
    "commits_count": 3,
    "message": "Индексация начата"
  }
}
```

### Индексация завершена:

```json
{
  "status": "ok",
  "project": "staffprobot",
  "indexing": {
    "status": "completed",
    "started_at": "2025-10-17T10:30:00",
    "completed_at": "2025-10-17T10:30:45",
    "ref": "refs/heads/main",
    "commits_count": 3,
    "stats": {
      "total_files": 156,
      "total_chunks": 2341,
      "errors": 0
    },
    "message": "Индексация успешно завершена"
  }
}
```

### Ошибка индексации:

```json
{
  "status": "ok",
  "project": "staffprobot",
  "indexing": {
    "status": "failed",
    "started_at": "2025-10-17T10:30:00",
    "failed_at": "2025-10-17T10:30:10",
    "ref": "refs/heads/main",
    "error": "git pull failed: Permission denied",
    "message": "Ошибка индексации: git pull failed"
  }
}
```

---

## 🚀 Преимущества

### До автообновления:

```bash
# Каждый раз после изменений:
git push
ssh server "cd /path/to/project && git pull"
ssh server "python scripts/reindex.py"  # ← РУЧНАЯ РАБОТА
```

### С автообновлением:

```bash
# Просто пушим - всё остальное автоматически!
git push
# ✅ Код обновлён
# ✅ База переиндексирована
# ✅ RAG отвечает актуально
```

**Экономия времени:** ~5-10 минут на каждый push

---

## 🔐 Безопасность

### Текущая защита:

1. ✅ **HMAC-SHA256 подпись** - проверка подлинности от GitHub
2. ✅ **Секретный ключ** - 64-символьный hex (256 бит)
3. ✅ **Timing-safe comparison** - защита от timing attacks
4. ✅ **Логирование** - все события записываются

### Что проверяется:

- Заголовок `X-Hub-Signature-256` от GitHub
- HMAC с секретом из `.env`
- Совпадение подписей через `hmac.compare_digest()`

### Если секрет скомпрометирован:

```bash
# 1. Сгенерировать новый
openssl rand -hex 32

# 2. Обновить в .env
sed -i 's/GITHUB_WEBHOOK_SECRET=.*/GITHUB_WEBHOOK_SECRET=<новый>/' .env

# 3. Обновить в GitHub webhook settings

# 4. Перезапустить контейнер
docker compose -f docker-compose.local.yml restart api
```

---

## 📈 Мониторинг

### В Project Brain:

```bash
# Статус всех индексаций
curl http://192.168.2.107:8003/api/webhook/status

# Логи в реальном времени
cd /home/sa/projects/project-brain
docker compose -f docker-compose.local.yml logs api -f
```

### В GitHub:

```
Settings → Webhooks → Recent Deliveries
```

Показывает:
- ✅ Успешные доставки (HTTP 200)
- ❌ Ошибки (4xx, 5xx)
- 📋 Request/Response payload

---

## 🎉 Итог

✅ **Всё готово к использованию!**

**Что работает:**
- Webhook endpoint с проверкой подписи
- API статуса индексации
- Автоматический git pull + индексация
- Безопасность через HMAC-SHA256
- Полная документация

**Следующий шаг:**
1. Добавить webhook в GitHub (5 минут)
2. Сделать тестовый push
3. Проверить автоиндексацию
4. Готово! База знаний всегда актуальна 🚀

---

**Документация:**
- Полная инструкция: `docs/GITHUB_AUTO_UPDATE_GUIDE.md`
- Текущий статус: `docs/GITHUB_WEBHOOK_STATUS.md` (этот файл)
- Анализ использования: `docs/USAGE_ANALYSIS.md`

