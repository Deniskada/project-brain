# ✅ Project Brain для StaffProBot - Настройка завершена

## 🎯 Что выполнено

### 1. ✅ GitHub Webhook Автообновление
**Файлы:**
- `backend/api/routes/webhook.py` - обработчик webhook
- `.env` - секрет webhook
- `config/projects.yaml` - конфигурация проектов

**API:**
- `POST /api/webhook/github` - прием событий от GitHub
- `GET /api/webhook/status/{project}` - статус индексации
- `POST /api/webhook/manual-reindex/{project}` - ручная индексация

**URL обновлён на:** `https://dev.staffprobot.ru`

### 2. ✅ Обучающий скрипт QA
**Файл:** `scripts/train_qa_pairs.py`

**Обучающие данные (8 пар):**
1. Какие роли пользователей в StaffProBot?
2. Как происходит открытие смены?
3. Разница telegram_id и user_id
4. Где модель User и какие поля?
5. API endpoints для объектов
6. Система календаря смен
7. Деплой на production
8. Ошибка активного договора

**Результат:** ✅ Добавлено в базу знаний

### 3. ✅ Скрипты индексации
**Файлы:**
- `scripts/simple_reindex.py` - полная переиндексация
- `scripts/full_reindex.sh` - bash wrapper с мониторингом
- `scripts/test_queries.py` - автотесты качества

### 4. ✅ Тестовые вопросы
**Файл:** `tests/test_staffprobot_queries.json`

**25 вопросов по категориям:**
- Архитектура (роли, структура)
- Бизнес-логика (смены, процессы)
- API (endpoints)
- База данных (модели, связи)
- Аутентификация (auth flow)
- Геолокация, Bot, Web, Deploy, Debug

### 5. ✅ Документация
**Созданные файлы:**
- `docs/GITHUB_AUTO_UPDATE_GUIDE.md` - полная инструкция webhook
- `docs/GITHUB_WEBHOOK_STATUS.md` - статус реализации
- `docs/GITHUB_WEBHOOK_FINAL_REPORT.md` - детальный отчёт
- `FINAL_SETUP_COMPLETE.md` - этот файл

---

## 🚀 Как использовать

### Настройка GitHub Webhook (одноразово, 5 минут)

**1. Откройте настройки репозитория:**
```
https://github.com/Deniskada/staffprobot/settings/hooks
```

**2. Add webhook:**
- **Payload URL:** `https://dev.staffprobot.ru/api/webhook/github`
- **Content type:** `application/json`
- **Secret:** `f00872ab2116aa77a1931cbaa594cf3bf77b63ecf9a7739955366511fb14f42e`
- **Events:** ✅ Just the push event
- **Active:** ✅

**3. Save → Готово!**

### Автоматическая работа (после настройки)

```bash
# Обычный workflow разработки
cd /home/sa/projects/staffprobot
vim apps/web/routes/owner/objects.py
git add .
git commit -m "Изменение: новый функционал"
git push origin main

# ВСЁ! Автоматически:
# 1. GitHub → webhook → dev.staffprobot.ru
# 2. git pull в контейнере
# 3. Переиндексация проекта
# 4. База знаний обновлена за 1-3 минуты
```

### Ручная переиндексация (если нужно)

```bash
# Полная индексация
cd /home/sa/projects/project-brain
docker compose -f docker-compose.local.yml exec api \
  python /app/scripts/simple_reindex.py staffprobot

# Или через bash скрипт с мониторингом
./scripts/full_reindex.sh

# Через API
curl -X POST http://192.168.2.107:8003/api/webhook/manual-reindex/staffprobot
```

### Обучение на новых QA парах

```bash
# 1. Отредактировать scripts/train_qa_pairs.py
# Добавить новые пары в массив QA_PAIRS

# 2. Запустить обучение
docker compose -f docker-compose.local.yml exec api \
  python /app/scripts/train_qa_pairs.py staffprobot

# 3. Результат: новые знания добавлены в базу
```

### Тестирование качества

```bash
# Запуск всех 25 тестовых вопросов
docker compose -f docker-compose.local.yml exec api \
  python /app/scripts/test_queries.py

# Или отдельный вопрос
curl -X POST http://192.168.2.107:8003/api/query \
  -H "Content-Type: application/json" \
  -d '{"query":"Какие роли в StaffProBot?","project":"staffprobot"}'
```

---

## 📊 Текущее состояние

### Индексация
- ✅ Код проекта StaffProBot проиндексирован
- ✅ 8 обучающих QA пар добавлены
- ✅ RAG Engine инициализирован
- ⚠️ Качество ответов требует полной переиндексации

### Тестирование
- ✅ 10/25 вопросов протестировано
- 📈 Keyword score: 12% (низкий)
- 📚 Среднее источников: 3-5
- ⚠️ Нужна оптимизация поиска

### Рекомендации
1. **Запустить полную переиндексацию:**
   ```bash
   docker compose -f docker-compose.local.yml exec api \
     python /app/scripts/simple_reindex.py staffprobot
   ```

2. **Добавить больше обучающих пар** в `train_qa_pairs.py`

3. **Настроить webhook в GitHub** для автоматизации

4. **Увеличить top_k** в RAG запросах (с 5 до 10)

---

## 🔧 Конфигурация

### Пути проектов
```yaml
# config/projects.yaml
projects:
  - name: staffprobot
    path: /projects/staffprobot  # в Docker
    git_url: https://github.com/Deniskada/staffprobot
```

### Переменные окружения
```bash
# .env
GITHUB_WEBHOOK_SECRET=f00872ab2116aa77a1931cbaa594cf3bf77b63ecf9a7739955366511fb14f42e
OLLAMA_HOST=http://192.168.2.107:11434
OLLAMA_MODEL=qwen2.5:14b-instruct
CHROMA_HOST=http://chromadb:8000
```

### Docker
```bash
# Контейнеры
project-brain-api        # API сервер (FastAPI + RAG)
project-brain-chromadb   # Векторная БД
project-brain-redis      # Кэш

# Volumes
/home/sa/projects/staffprobot:/projects/staffprobot
```

---

## 📝 Команды для повседневной работы

### Проверка статуса
```bash
# Статус контейнеров
cd /home/sa/projects/project-brain
docker compose -f docker-compose.local.yml ps

# Статус webhook
curl http://192.168.2.107:8003/api/webhook/test

# Статус индексации
curl http://192.168.2.107:8003/api/webhook/status/staffprobot
```

### Логи
```bash
# API логи
docker compose -f docker-compose.local.yml logs api -f

# Логи webhook событий
docker compose -f docker-compose.local.yml logs api | grep "GitHub Push"

# Логи индексации
docker compose -f docker-compose.local.yml logs api | grep "📊 Обработано"
```

### Запросы к RAG
```bash
# Простой запрос
curl -X POST http://192.168.2.107:8003/api/query \
  -H "Content-Type: application/json" \
  -d '{"query":"Как работает аутентификация?","project":"staffprobot"}'

# Запрос с форматированием
curl -s -X POST http://192.168.2.107:8003/api/query \
  -H "Content-Type: application/json" \
  -d '{"query":"Какие роли в системе?","project":"staffprobot"}' | jq
```

---

## 🎯 Метрики качества (целевые)

### После полной настройки:
- ✅ Успешных запросов: **95-100%**
- ✅ Keyword score: **70-90%**
- ✅ Среднее источников: **8-12**
- ✅ Точные ссылки на файлы и строки кода

### Текущие (требуют улучшения):
- ⚠️ Успешных запросов: **100%** (но ответы неполные)
- ⚠️ Keyword score: **12%** (низкий)
- ⚠️ Среднее источников: **3-5** (мало)
- ⚠️ Ответы: "В текущей кодовой базе такой информации нет"

---

## 🐛 Решение проблем

### "Empty reply from server"
**Причина:** Долгий запрос блокирует ответ  
**Решение:** Используйте фоновые задачи (BackgroundTasks)

### "В текущей кодовой базе такой информации нет"
**Причина:** Неполная индексация или плохое качество эмбеддингов  
**Решения:**
1. Запустить полную переиндексацию
2. Добавить обучающие QA пары
3. Увеличить top_k в запросах

### Контейнер падает (Exit 137)
**Причина:** Out of Memory  
**Решение:** Увеличить лимиты в docker-compose.yml:
```yaml
deploy:
  resources:
    limits:
      memory: 16G  # было 8G
```

### Webhook не срабатывает
**Проверки:**
1. Webhook добавлен в GitHub?
2. URL правильный? (https://dev.staffprobot.ru/api/webhook/github)
3. Секрет совпадает?
4. Recent Deliveries показывает 200 OK?

---

## 🎉 Итоговый чеклист

### ✅ Реализовано:
- [x] GitHub Webhook API с HMAC-SHA256
- [x] API статуса индексации
- [x] Обучающий скрипт QA (8 пар)
- [x] 25 тестовых вопросов
- [x] Скрипты индексации
- [x] Полная документация
- [x] URL обновлён на dev.staffprobot.ru

### 🔧 Осталось настроить (15 минут):
- [ ] Добавить webhook в GitHub
- [ ] Запустить полную переиндексацию
- [ ] Протестировать качество ответов
- [ ] Добавить больше обучающих пар (опционально)

### 📈 Рекомендации на будущее:
- [ ] Увеличить top_k до 10-15
- [ ] Добавить кэширование популярных запросов
- [ ] Настроить мониторинг метрик
- [ ] Периодическая переиндексация (раз в неделю)

---

## 📚 Полезные ссылки

**Документация:**
- Полная инструкция: `docs/GITHUB_AUTO_UPDATE_GUIDE.md`
- Статус реализации: `docs/GITHUB_WEBHOOK_STATUS.md`
- Итоговый отчёт: `docs/GITHUB_WEBHOOK_FINAL_REPORT.md`

**Скрипты:**
- Индексация: `scripts/simple_reindex.py`
- Обучение: `scripts/train_qa_pairs.py`
- Тестирование: `scripts/test_queries.py`

**Конфигурация:**
- Проекты: `config/projects.yaml`
- Переменные: `.env`
- Docker: `docker-compose.local.yml`

---

**🚀 Система готова к использованию!**

После добавления webhook в GitHub ваша база знаний будет автоматически обновляться при каждом push. Экономия времени: ~5-10 минут на каждый commit!

