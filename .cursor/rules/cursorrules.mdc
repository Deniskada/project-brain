# Project Brain - Правила разработки

## 🎯 Цель проекта
Система управления знаниями с RAG для индексации кода, AI-ассистента и генерации документации.

## 🏗️ Архитектура

### Структура
```
backend/
  api/           # FastAPI endpoints (routes, main.py)
  rag/           # RAG engine (ChromaDB, embeddings)
  llm/           # Ollama client (промпты, параметры)
  indexers/      # Python/Markdown парсеры
  generators/    # Документация, экспорт
  analyzers/     # Анализ структуры проектов
frontend/        # HTML/JS чат-интерфейс
config/          # projects.yaml - конфигурация проектов
```

### Технологии
- **Backend**: FastAPI + async/await
- **RAG**: ChromaDB 0.5.x + sentence-transformers
- **LLM**: Ollama (qwen2.5:14b-instruct)
- **Индексация**: AST парсинг (tree-sitter подобно)
- **Docker**: docker-compose.local.yml

## 🚫 КРИТИЧЕСКИЕ ЗАПРЕТЫ

### 1. НЕ создавать helper скрипты
- ❌ Не создавать временные скрипты для обхода проблем
- ✅ Использовать стандартные инструменты и исправлять root cause

### 2. НЕ галлюцинировать
- ❌ Не выдумывать файлы, которых нет в проекте
- ❌ Не предлагать код из несуществующих модулей
- ✅ ВСЕГДА проверять файлы через read_file/grep перед предложениями

### 3. НЕ дублировать коллекции
- ❌ Не создавать `project_brain` коллекцию (старая, удалить!)
- ✅ Использовать `kb_{project_name}` для каждого проекта
- ✅ Вызывать `get_collection(project)` перед работой с ChromaDB

### 4. НЕ хардкодить проекты
- ❌ `project: str = "staffprobot"` в промптах
- ✅ Передавать `project_name` в `generate_response()`
- ✅ Динамические описания проектов

## ✅ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

### RAG Engine (`backend/rag/engine.py`)
```python
# ВСЕГДА получать коллекцию для проекта
collection = self.get_collection(project)

# НИКОГДА не использовать self.collection напрямую
# ❌ self.collection.query(...)
# ✅ collection = self.get_collection(project); collection.query(...)
```

### LLM Client (`backend/llm/ollama_client.py`)
```python
# ОБЯЗАТЕЛЬНО передавать project_name
await ollama.generate_response(
    query=query,
    context=context,
    project_name=project  # ← КРИТИЧНО!
)

# Параметры для точности (НЕ МЕНЯТЬ без тестирования):
temperature=0.3      # Меньше галлюцинаций
repeat_penalty=1.2   # Без повторов
num_ctx=4096        # Больше контекста
timeout=90          # Для 14B модели
```

### Промпты - анти-галлюцинация
```python
"""
КРИТИЧЕСКИ ВАЖНО:
- Контекст относится ТОЛЬКО к проекту {project_name}
- НИКОГДА не упоминай другие проекты
- НИКОГДА не выдумывай файлы
- Если файла нет в контексте - скажи "Такой информации нет"
"""
```

### Intent Detection
```python
# Обязательные интенты:
'overview'   → doc_type: 'documentation' (README)
'how_to'     → doc_type: 'route', 'handler'
'structure'  → doc_type: 'model', 'schema'
'api'        → doc_type: 'route', 'api'
```

### Приоритет типов документов
```python
context_order = [
    'documentation',  # ПЕРВЫЙ! (README, описания)
    'route', 'handler', 'api',
    'service', 'form',
    'model', 'schema',
    'other'
]
```

## 📝 Стиль кода

### Python
- **Type hints**: ОБЯЗАТЕЛЬНО для всех функций
- **Async/await**: для I/O операций (ChromaDB, Ollama)
- **Logging**: `logger.info/error`, НЕ print()
- **Docstrings**: для публичных методов

### Именование
- `snake_case` для функций/переменных
- `PascalCase` для классов
- `UPPER_CASE` для констант
- Префикс `_` для приватных методов

### Обработка ошибок
```python
try:
    result = await operation()
except SpecificError as e:
    logger.error(f"Описание: {e}", exc_info=True)
    raise  # Или обработать
```

## 🔄 Workflow

### Перед изменениями
1. `grep` или `codebase_search` для поиска существующего кода
2. `read_file` для изучения контекста
3. Предложить решение → дождаться подтверждения

### После изменений
1. Проверить linter errors
2. Перезапустить контейнер (если нужно)
3. Тестировать через веб-интерфейс или API
4. Коммит с кратким описанием на русском

### Docker
```bash
# Перезапуск API
docker compose -f docker-compose.local.yml restart api

# Логи
docker logs project-brain-api --tail 50

# Индексация
curl -X POST http://localhost:8003/api/index/{project_name}
```

## 🧪 Тестирование

### Веб-интерфейс
- URL: http://192.168.2.107:8003/chat
- Выбрать проект в селекторе
- Проверить ответ, источники, время

### API
```bash
curl -X POST http://localhost:8003/api/query \
  -H "Content-Type: application/json" \
  -d '{"query": "вопрос?", "project": "staffprobot"}'
```

### Проверка коллекций
```bash
docker exec project-brain-api python3 -c \
  "import chromadb; client = chromadb.HttpClient(host='chromadb', port=8000); \
   [print(f'{c.name}: {c.count()} docs') for c in client.list_collections()]"
```

## 📚 Документация

### Обновлять при изменениях
- `README.md` - для внешних пользователей
- `doc/project-status.md` - прогресс разработки
- `FINAL_STATUS.md` - текущий статус системы
- `IMPROVEMENTS_LOG.md` - лог улучшений

### Коммиты
- Формат: `"Краткое описание на русском"`
- Примеры:
  - ✅ "Раздельные коллекции ChromaDB"
  - ✅ "Исправление галлюцинаций в промптах"
  - ❌ "fix bug" (слишком общее)

## 🎯 Приоритеты качества

1. **Точность** > скорость (лучше медленно, но правильно)
2. **Простота** > clever code (KISS принцип)
3. **Явность** > неявность (explicit is better than implicit)
4. **Тестируемость** > красота кода

## 💡 Специфика проекта

### Мультипроектность
- Каждый проект в своей коллекции `kb_{project}`
- НЕ смешивать контекст между проектами
- Передавать `project` во все методы RAG

### Ollama + GPU
- Модель на хосте (не в Docker): `192.168.2.107:11434`
- Модель: `qwen2.5:14b-instruct` (8.9GB)
- НЕ переключать на 32B без согласования (слишком медленно)

### ChromaDB 0.5.x
- Новый API: `HttpClient`, НЕ старый `Client`
- Коллекции: `get_or_create_collection()`
- Уникальные ID: `f"{project}_{chunk_id}"`

## 🔗 Полезные ссылки

- Ollama модели: https://ollama.com/library
- ChromaDB docs: https://docs.trychroma.com/
- sentence-transformers: https://www.sbert.net/

---

**Помни**: Quality > Speed. Лучше спросить, чем выдумать!

